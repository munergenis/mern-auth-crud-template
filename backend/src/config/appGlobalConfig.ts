// Reset emails requested per user in the minute range limit
export const RESET_EMAILS_RATE_LIMIT = 2;
// Minute Range users can request the above number of reset emails
export const MINUTES_RANGE_RATE_LIMIT = 5;
